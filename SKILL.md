---
name: daily-changelog
description: 自动化分析代码变更，生成中文产品更新日志。
---

# 全栈产品更新日志生成器

## 参数

| 参数 | 行为 |
|------|------|
| 无参数 | 分析今天 |
| `YYYY-MM-DD` | 分析指定日期 |
| `YYYY-MM-DD YYYY-MM-DD` | 分析日期区间 |
| `--repos` | 多仓库分析，格式: `name:path,name:path` |

---

## 执行流程

### Step 1：运行分析脚本

**单仓库模式**：
```bash
python3 scripts/context_fetcher.py \
  --since $SINCE --until $UNTIL \
  --repo-path /path/to/repo > /tmp/changelog_report.txt
```

**多仓库模式**（推荐，用于全栈项目）：
```bash
python3 scripts/context_fetcher.py \
  --since $SINCE --until $UNTIL \
  --repos "backend:/path/to/backend,frontend:/path/to/frontend" > /tmp/changelog_report.txt
```

输出 `NO_CHANGES` 则终止。

### Step 2：读报告

报告头部会显示：
- 各仓库的变更文件数量
- 变更类型统计（🐛Bug修复、✨新功能、🔧技术改造、📝其他）
- 产品主题候选（从路由、页面、DTO、Query、Enum、服务名中提取）

**重点阅读**（按优先级）：

1. **新增的入口层代码**（Controller / DubboService / 前端路由）
2. **新增的数据库表结构**（SQL 迁移文件）
3. **接口变更**（新增/删除/修改的路由和方法）
4. **Breaking Change 标记**
5. **产品信号**（新增字段、筛选条件、指标、权限范围、按钮/标题文案）

### Step 3：理解产品变更

阅读 diff 时，将技术变更转化为产品含义：

| 技术信号 | 产品含义 |
|---------|---------|
| 新增 HTTP 接口 | 用户获得某种能力 |
| 新增前端路由 | 新页面上线 |
| 新增 Dubbo 接口 | 内部服务能力变化 |
| 新增定时任务 | 后台自动化处理 |
| SQL 迁移 | 数据结构变化 |
| 配置类变更 | 功能开关/灰度变化 |
| DTO/VO 新增字段 | 展示字段、返回内容或请求参数可能变化 |
| Query 新增字段 | 新筛选条件或统计口径可能变化 |
| Enum 新增值 | 新业务类型、状态或选项可能上线 |
| 前端按钮/标题/表格列 | 页面操作入口、字段展示或筛选项可能变化 |

**描述规则**：
- ❌ 错误：新增了 `/api/example/action` 接口
- ✅ 正确：详情页新增操作入口，用户可直接发起处理流程

- ❌ 错误：前端新增 ExamplePage 组件
- ✅ 正确：新增申请页面，用户可填写信息并提交

### Step 4：生成日志

读取 `references/changelog_template.md`，按模板输出。

**严格输出规则**：

1. **日期规则**：
   - 如果某天没有任何实质产品变更内容，则**整一天不输出**
   - 不要为了凑数而输出空洞的日期

2. **分类规则**：
   - 如果某分类（如"技术改造"）没有内容，**该分类整块不输出**
   - 不要保留空的分类标题

3. **内容规则**：
   - 每条内容必须包含：**用户能做什么** 或 **系统发生了什么变化**
   - 每条内容必须包含至少一个**场景锚点**：页面、模块、接口域、用户角色、操作场景
   - 不要只列出文件名或接口路径
   - 不要写"后端新增 X 个功能提交"这种无意义的内容
   - 优先把同一主题的前后端、SQL、DTO、页面改动归并成一条
   - 如果只能确认到能力层，写“支持按...查看...”或“新增...能力”，不要写技术实现名
   - 禁止在最终日志中出现文件路径、文件名、类名、方法名、组件名
   - 像 `ExamplePage.tsx`、`ExampleController`、`ExampleQueryService` 这类技术名只能作为分析证据，不能直接写进结果
   - 像“参数错误码”“系统异常”“DTO 字段”这类研发术语，要翻译成用户能理解的话
   - 如果找不到场景锚点，不要强行输出正式产品日志，可降级为“后台某能力优化（具体入口待确认）”

4. **禁止事项**：
   - 不要使用 `generate_yearly_changelog.py`
   - 不要输出 `### 📍 接口变更` 之类模板外分类
   - 不要输出“修改了 X 个文件 / X 个提交”这类开发视角表述

**正确的输出示例**：

```markdown
## 2026-03-11

### ✨ 新功能

- 列表页增加状态筛选能力，用户可按条件快速定位目标记录

### 🐛 Bug 修复

- 修复统计页中的时间排序异常，最近数据展示更准确

---

## 2026-03-09

### ✨ 新功能

- 数据看板新增趋势查看能力，支持按关键指标观察变化情况
```

**错误的输出示例**（不要这样写）：

```markdown
## 2026-03-11

### ✨ 新功能

- 后端新增 2 个功能提交

### 📍 接口变更

- ExampleController.java
- ExampleQueryService.java
```

---

### Step 5：保存

将生成的日志保存到用户指定的位置。
